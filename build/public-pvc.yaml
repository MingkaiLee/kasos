apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: public-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
    limits:
      storage: 2Gi
  storageClassName: local-storage